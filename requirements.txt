pymysql>=1.1.0
psutil>=5.9.0
# pynacl: libsodium binding usado pelo módulo MEGA (app/mega_uploader.py)
# para decifrar credenciais entregues pelo painel (XSalsa20-Poly1305 secretbox).
pynacl>=1.5.0
# certifi: bundle de certificados-raiz embutido no .exe (app/config.py força o
# HTTPS a validar contra ele em vez do repositório do Windows). Corrige
# "certificate has expired" em PCs sem os roots novos da Let's Encrypt.
certifi>=2024.2.2
